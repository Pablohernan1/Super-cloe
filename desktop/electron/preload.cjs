// Vacío por ahora -- la app habla directo con Supabase desde el renderer
// (contextIsolation queda activo por seguridad estándar de Electron, pero
// no exponemos ningún API de Node al renderer todavía).
